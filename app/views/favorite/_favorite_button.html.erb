

<%
=begin
%>


 <%#  デバック用　　中身を確認　ここから　　　%>　　
<p>current_user.id : <%= current_user.id.inspect %> </p>
<p>micropost.id : <%= micropost.id.inspect %></p>
<p>current_user.myfavorite? : <%= current_user.myfavorite?(micropost) %> </p>
<%#  デバック用　　中身を確認　ここまで　　　%>　　

<%
=end
%>

  <% if current_user.myfavorite?(micropost) %>
    <%= form_with(model: current_user.favorites.find_by(micropost_id: micropost.id), local: true, method: :delete) do |f| %>
      <%= f.submit 'Remove Myfavorite', class: 'btn btn-danger btn-sm' %>
    <% end %>
  <% else %>
  <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
    <%= hidden_field_tag :micropost_id, micropost.id %>
    <%= f.submit 'Add Myfavorite', class: 'btn btn-primary btn-block' %>
    <% end %>
  <% end %>



